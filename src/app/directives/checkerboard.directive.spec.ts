import { CheckerboardDirective } from './checkerboard.directive';

describe('CheckerboardDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckerboardDirective();
    expect(directive).toBeTruthy();
  });
});
